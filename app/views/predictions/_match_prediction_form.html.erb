<%# Local variables: match, prediction, betting_pool, show_stage (optional) %>
<% show_stage ||= false %>

<%= render "predictions/#{match.match_type}_form",
           match: match,
           prediction: prediction,
           betting_pool: betting_pool,
           show_stage: show_stage %>
